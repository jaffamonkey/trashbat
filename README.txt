Files generated from Wayback Machine text captures with toolbar/frame content removed.
Note: this environment could not download the raw source HTML directly, so these files preserve the parsed visible HTML/text content rather than the original source markup/images.
